<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huantu.mapper.FavoriteMapper">

    <resultMap id="favoriteMap" type="com.huantu.entity.Favorite">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="targetId" column="target_id"/>
        <result property="targetType" column="target_type"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO `favorite` (user_id, target_id, target_type, created_at)
        VALUES (#{userId}, #{targetId}, #{targetType}, NOW())
    </insert>

    <select id="findByUserAndTarget" resultMap="favoriteMap">
        SELECT * FROM `favorite`
        WHERE user_id = #{userId} AND target_id = #{targetId} AND target_type = #{targetType}
    </select>

    <delete id="deleteById">
        DELETE FROM `favorite` WHERE id = #{id}
    </delete>

    <select id="findByUser" resultMap="favoriteMap">
        SELECT * FROM `favorite`
        WHERE user_id = #{userId}
        <if test="targetType != null">AND target_type = #{targetType}</if>
        ORDER BY created_at DESC
    </select>

    <select id="countByUser" resultType="int">
        SELECT COUNT(*) FROM `favorite` WHERE user_id = #{userId}
    </select>

</mapper>
